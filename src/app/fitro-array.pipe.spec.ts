import { FitroArrayPipe } from './fitro-array.pipe';

describe('FitroArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FitroArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
